import 'package:flutter/material.dart';

class Audio1 extends StatelessWidget {
  const Audio1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
      body: Center(
        child: Text("Audio"),
      ),
    );
  }
}
